<UserControl x:Class="Postman.Views.ResponseBodyRawUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Postman.Views"
             mc:Ignorable="d">
    <Grid>
        <!--<TextBox Margin="20,10,10,10" Text="{Binding ResponseContent, UpdateSourceTrigger=PropertyChanged}" MaxHeight="400" ScrollViewer.VerticalScrollBarVisibility="Auto" IsReadOnly="True" TextWrapping="Wrap" AllowDrop="True"/>-->

        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="350" Margin="20,10,10,10">
            <TextBlock Text="{Binding ResponseContent, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap"/>
        </ScrollViewer>

        <!--<FlowDocumentReader/>-->
    </Grid>
</UserControl>
